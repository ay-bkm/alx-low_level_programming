#include "main.h"
/**
 * _strlen - calculates the length of string
 * @s: the string checked
 * Return: length of string
 */

int _strlen(char *s)
{
	int c = 0;
	while (s[a] != '\0')
	{
		++c;
	}
	return (c);
}
